<% if has_flash? %>
  <div class="<%= flash_class %>">
    <p><%= flash_notice %></p>
  </div>
<% end %>

<h1>Editing Goodie</h1>

<%= render 'form' %>

<hr>

<div class="btn-group">
  <%= link_to 'Back', goodies_path, class: 'btn btn-default', role: 'button' %>
  <%= link_to 'Show', @goodie, class: 'btn btn-default', role: 'button' %>
</div>
